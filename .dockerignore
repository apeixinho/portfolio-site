# VCS
.git
.gitignore

# Docker (build uses Dockerfile on host; no need inside image context)
Dockerfile
dockerfile
.dockerignore

# Build output (rebuilt in container)
dist
tmp

# Dependencies (installed in image: full install in builder, --omit=dev in runtime stage)
node_modules
bower_components

# Editors / OS
.idea
.vscode
.cursor
.project
.classpath
*.launch
.editorconfig
.settings/
.DS_Store
Thumbs.db

# Logs & caches
.sass-cache
coverage
npm-debug.log*
testem.log
typings
libpeerconnection.log

# Docs (not needed to build)
*.md
LICENSE

# Other package managers (this project uses package-lock.json + npm ci in Docker)
yarn.lock
pnpm-lock.yaml

# Misc
connect.lock
e2e/*.js
e2e/*.map
